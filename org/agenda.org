#+title: Agenda Notebook
#+author: Marcus Birkenkrahe
#+subtitle: CS 101 - Catholic Polytechnic University - Spring 2026
#+SEQ_TODO: TODO NEXT IN-PROGRESS | DONE CANCELLED
#+startup: overview hideblocks indent entitiespretty
#+property: header-args:C      :main yes :includes <stdio.h> :results output :exports both
#+property: header-args:python :session *Python* :python python3 :results output :exports both
#+property: header-args:R      :session *R* :results graphics output file :exports both
#+property: header-args:C++    :main yes :includes <iostream> :results output :exports both
[[../img/cover.png]]

* Opening and Closing Prayers
** Opening Prayer
#+attr_html: :width 400px :float nil:
[[../img/opening_prayers.png]]

** Closing Prayer
#+attr_html: :width 400px :float nil:
[[../img/closing_prayers.png]]

** Why these prayers?

- *The "Our Father"*, because we learn everything through him.
- *The "Hail Mary"*, because we need trust and humility to learn.
- *St. Albert the Great*, because he is the patron saint of scientists,
  philosophers, and students of the natural sciences.
- *St. Thomas Aquinas*, because he is the patron saint of Catholic
  schools, universities, and scholars.
- *St. Isidore of Seville*, because he is the patron saint of students,
  scholars, the internet, computer users and encyclopedias.
- *A student's prayer* is by St. Thomas Aquinas.

* Week 1 - Course overview and first steps

- [X] *Opening prayer* 5'
- [X] *Introductions*: Say a few words about yourself!
- [X] *Discussion*: Way of the course (review entry survey) - Times?
- [X] *Lecture:* [[file:0_overview.org][Course overview]] (see also syllabus) 75' + 15'
- [X] *Practice*: Tooling (OneCompiler, Google Cloud Shell) 60'
- [X] *Dark horse tip*: Emacs + Org-mode (optional but useful).

** Summary of the tool session

- You have to include ~<iostream>~ to print or get data.
- In C++, data can be streamed anywhere (using ~std::cout~).
- OneCompiler.com is useful because it offers multiple languages.
- With Google Cloud Shell you can spin up a Linux VM fast.
- You should always ask students to fix other students' mistakes.

* Week 2 -  First assignment feedback & C++ tutorial
#+attr_html: :width 400px :float nil:
#+caption: Illustrating the current AI hype level
[[../img/michelangelo.png]]

- [ ] *Opening prayer* 5'
- [ ] *Housekeeping*
- [ ] *Practice*: 10 simple C++ vs. Java vs. Python programs
- [ ] *Asynchronous:* Watch remaining Java vs. C++ demos?
- [ ] *Assignment*: Finish remaining practice problems?
- [ ] *Bonus Assignment*: 10 more super simple C++ sample programs
- [ ] *Quiz 2*: MPC questions based on lecture and practice content
- [ ] *Session summary* (together)
- [ ] *Closing prayer* 5'

** Housekeeping
#+attr_html: :width 700px :float nil:
#+caption: Generated by notebooklm.google.com using ChatGPT prompt
[[../img/info_session_1_30_jan_2026.png]]

I will always summarize the session and assignments on Saturday.

I will attempt to give you feedback during the week. Try me!

- [X] *Canvas:* Post-mortem message: Assignment/session summary
- [X] *Books* on C and C++
- [X] Further reading: *Linux* command-line, Literate programming
- [X] How *AI* use affects coding skills (of professionals)
- [X] The complete compiler chain - BPMN diagram (~gcc~ / ~g++~)
- [X] *Video*: How to compile "Hello, world!" on a Linux terminal
- [X] Reminder for quiz 1 (is an announcement/email better?)
- [X] Manager position for AI bot team (job advert)
- [X] "Occasional discomfort with being a product" (AI = machine)

** Review: session 1

I put these together after looking at the *compilation practice* script.

1. What does ~OneCompiler.com~ not show?
   #+begin_quote
   The compilation of the program. It's an IDE that gives you the
   illusion that C++ is an interpreted language (like Python).
   #+end_quote

2. What does the command =./a.out= mean precisely?
   #+begin_quote
   To the *shell* program, it means to look for =a.out= in the current (=.=)
   directory if the current directory is not already in the ~$PATH~.
   #+end_quote

3. Can you remember one true Linux command and what it means?
   #+begin_src bash :results output :exports both
     ls              # `ls` lists files
     cd ..           # `cd` changes directory (.. -> next up)
   #+end_src

   #+RESULTS:
   #+begin_example
   0_overview.org
   0_practice.org
   1_java2cpp.org
   agenda25.org
   agenda.html
   agenda.org
   architecture.org
   args
   args.cpp
   compare
   cpp_study_sheet.org
   cpp_study_sheet.pdf
   entry_pgm_solutions.org
   input
   java2cpp_class.org
   Main.class
   main.cpp
   Main.java
   syllabus.html
   syllabus.org
   tests.org
   #+end_example

4. Does every C++ program have to have ~main~? What is ~main~?
   #+begin_quote
   Yes. ~main~ is a function and the entry point to the program proper,
   preceeded by the preprocessor directives (like ~#include~).
   #+end_quote

6. What does ~void~ mean and where does it occur?
   #+begin_quote
   ~void~ as in ~main(void)~ means that no arguments will be expected. As
   a ~return~ value, ~void~ means "do not ~return~ any value."
   #+end_quote

** Bonus assignment "Program translator" (Q&A)

You had to translate a Java (or a Python) program into C++. The
program defined two variables and printed them using ~std::cout~.

- Your question (paraphrased):
  #+begin_quote
  What is the ~<<~ doing?
  #+end_quote

- *Answer:* We'll discuss this later in class: ~<<~ is the stream
  insertion operator. It inserts data into the output stream (that
  streams data to any device, e.g. a screen), represented by ~cout~, the
  standard output (~stdout~) object.

  So the chain printout command ~cout << "Hello" << 42 << endl;~
  1. inserts the string "Hello" into ~cout~
  2. inserts the integer 42 into ~cout~
  3. inserts ~endl~ (= newline + flush) into ~cout~.

  The idea of using a data stream makes data transfer (to the outside
  world) more general but also conceptually more difficult than just a
  C-style function like ~printf~, or a Java function like ~println~.

- Your question (paraphrased):
  #+begin_quote
  For the ~main~ program, I didn't use an argument like ~String[] args~. Why
  don't I need that in C++? Or should I have included it?
  #+end_quote

- *Answer:* You don't need it in C++, and you also don't need it in Java
  unless you want to pass arguments on the command-line to the program.

  The ~main~ function has an equivalent to the Java class argument
  ~String[] args~. It is ~main(int argc, char* argv[])~, and has two
  arguments that are actually easy to understand:
  1. ~argc~ is the number of arguments fed to the program
  2. ~argv[]~ is an array of strings that holds the arguments (an array
     of pointers to the beginning of each argument string) beginning
     with ~argv[0]~, which is the name of the program, and continuing
     with as many arguments that are passed to ~main~.

  *Background:* Java is tightly coupled to the JVM, the system that is
  shipped with it and ensures its portability (but also its
  slowness). C++ is closer to the operating system, which simply
  starts your program and optionally provides arguments.

- A *code example* use with command-line arguments looks like this:
  
  First the source code:
  #+begin_src C++ :tangle args.cpp :results none
    #include <iostream>
    #include <string>

    int main(int argc, char* argv[]) {
      // declare two strings
      std::string firstName, lastName;

      // print first command-line argument
      std::cout << "First name: " << argv[1] << std::endl
                << "Last name: "  << argv[2] << std::endl;
    }      
  #+end_src

  The source code file =args.cpp= (tangled from the code cell above) is
  compiled, and then run on the command-line with two string
  arguments:

  #+begin_src bash :results output :exports both
    ls -l args.cpp  # check if the file exists
    g++ args.cpp  -o args # compile and output executable `args`
    ./args Marcus Birkenkrahe
  #+end_src

  #+RESULTS:
  : -rw-rw-r-- 1 aletheia aletheia 285 Feb  2 21:18 args.cpp
  : First name: Marcus
  : Last name: Birkenkrahe

** 10 super simple sample C++ programming problems
#+attr_html: :width 700px :float nil:
[[../img/info_session_2_feb_6_2026.png]]

Topics to cover:
1. Variable declarations
2. Integer and floating-point arithmetic
3. Formatting output (~cout~, ~printf~)
4. Handling user input (~cin~)
5. Variable type conversion (un/safe)
6. Conditionals (~if~, ~else~, ~else if~, ~switch~)
7. Loops (aka *iterators* - ~for~, ~while~, ~do-while~)
8. Arrays (aka *containers* of collections)
9. Functions (aka *transformations*)
10. Libraries (~<iostream>~, ~<string>~, ~<iomanip>~)

How we will cover them:
- Demo from me (Java + C++)
- You practice a simple example

What you will get later:
- Study sheet (PDF)
- Video if we don't finish today (likely)
- Bonus assignment: 10 more simple sample programs
- Quiz to test your understanding
- Assignment: submit ZIP file with all solutions

** Notes

- [ ] Send NotebookLM demo script
- [ ] Send information on input/output streams
- [ ] More information on why programming languages for what
- [ ] Do a demo of pythontutor.com (online debugger)
- [ ] Better information about where students can find what

** Summary - one thing you learnt today

- ~printf~ requires specific format codes to print certain types:
  e.g. ~%i~ for ~int~, and ~%f~ for ~float~.

- Programming languages are created for a specific purposes, e.g.
  Python to do everything badly (but good enough mostly), or C++ to
  work with computer memory.

- C++ is a lot faster than all other languages (except C) - I'll send
  an illustration for that.

- The streaming operator ~<<~ or ~>>~ depends on what you want to do: ~cin
  >>~ for input and ~cout <<~ for output (like a pipe).

- All files are in github.com/birkenkrahe/cs101


